#!/usr/bin/env node

var program = require('commander');

program
    .version(require('../package').version)
    .usage('<command> [options]')
    .command("init", "generate a new project")
    .command("list", "list all project")
    .parse(process.argv);